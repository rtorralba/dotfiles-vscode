#!/bin/bash

cd $WORKSPACE_FOLDER
rm -rf .vscode/*
cp -r "$HOME/dotfiles/.vscode/*" .vscode/*
mkdir "$HOME/.config"
ln -s "$HOME/dotfiles/.config/phpactor" "$HOME/.config/phpactor"

composer global require friendsofphp/php-cs-fixer
